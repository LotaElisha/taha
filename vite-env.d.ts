/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />
// Sentry types come from `@sentry/react` once installed; when missing we
// dynamically import in lib/sentry.ts so TypeScript doesn't need a shim here.
